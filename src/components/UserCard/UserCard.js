import * as React from 'react'

import * as styles from './UserCard.module.css'

const UserCard = ({ user }) => {

    // const {name, rate, bio, image} = user

    return (
        <div className={`user-card ${styles.card}`}>

        </div>
    )
}

export default UserCard